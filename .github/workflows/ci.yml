name: Build, test and package
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  compile:
    name: Compile (${{ matrix.configuration }})
    runs-on: windows-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        configuration: [Debug, Release]
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore DLH.Controls.sln
      - name: Build ${{ matrix.configuration }}
        run: dotnet build DLH.Controls.sln -c ${{ matrix.configuration }} --no-restore
  validate:
    needs: compile
    runs-on: windows-latest
    timeout-minutes: 20
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      - name: Build, test and pack
        run: ./eng/Validate.ps1
      - name: Upload test logs and preview
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ github.run_number }}
          path: artifacts/test-results/
          if-no-files-found: warn
          retention-days: 14
      - name: Upload NuGet package
        uses: actions/upload-artifact@v7
        with:
          name: DLH.Controls.Wpf-${{ github.run_number }}
          path: artifacts/packages/*.nupkg
          if-no-files-found: error
          retention-days: 14
